#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double x=30.50;
    // double y=18;
    double z;
    // z= max(x, y);
    //z=min(x, y);
    // z= sqrt(9);
    //z=abs(-666);
     z=round(x);
    // z=floor(x);
    // z=ceil(x);
    cout<<z;
 

    return 0;
}